# UI Spec: [Surface name]

**Layout shape:** [single-column / two-column / master-detail / three-panel / dashboard / wizard / other] — because: [one sentence tied to the task]

**Design system in use:** [ATK UI / Web Awesome / existing project system] — [confirmed accessible / fallback used, and why]

**Tokens**
- Typography: [role tokens used]
- Color: [semantic tokens used — status/category distinctions listed]
- Spacing: [scale used, any density adjustment]

**Components** (existing vs. custom)
| Element | Component used | Existing or custom? |
|---|---|---|
| | | |

**Responsive plan**
- Desktop: [primary layout]
- Tablet: [what changes]
- Mobile: [what changes, what moves to a drawer/secondary tab]

**Interaction states covered:** default / hover / focus / active / disabled — [confirmed for which components]

**Visual density:** [dense / moderate / sparse] — because: [task/audience reason]

**Custom decisions (not in the existing system):**
- [decision] — reason: [why the existing system didn't cover this]
