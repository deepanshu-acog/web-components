---
// A full-height workbench / split-view layout for interactive data exploration,
// 3D molecular structures (<atk-molstar>), and tabular data grids (<atk-data-table>).
import "@fontsource-variable/ibm-plex-sans";
import "@fontsource-variable/jetbrains-mono";
import "@awesome.me/webawesome/dist/styles/themes/default.css";
import "@awesome.me/webawesome/dist/styles/utilities.css";
import "@aganitha/atk-ui/theme.css";
import "@aganitha/atk-ui/patterns.css";
import package_json from "../../package.json";

interface Props {
  title: string;
}

const { title } = Astro.props;
const build_time = new Date().toISOString();
---

<html lang="en" class="wa-theme-default atk-theme">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
    <script is:inline>
      (() => {
        const saved = localStorage.getItem('wa-theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        if (saved === 'dark' || (!saved && prefersDark)) {
          document.documentElement.classList.add('wa-dark');
        }
      })();
    </script>
  </head>
  <body>
    <wa-page mobile-breakpoint="768">
      <header slot="header" class="wa-split">
        <a href="/" class="brand">
          <wa-icon name="flask"></wa-icon> {title}
        </a>
        <span class="wa-cluster">
          <a class="search-link" href="/search">Search <kbd data-shortcut-hint>⌘K</kbd></a>
          <wa-button href="/help" appearance="plain">
            <wa-icon slot="start" name="circle-question"></wa-icon>Help
          </wa-button>
          <wa-button id="theme-toggle" appearance="plain" size="small" aria-label="Toggle dark mode" title="Toggle dark mode">
            <wa-icon name="moon" id="theme-toggle-icon"></wa-icon>
          </wa-button>
        </span>
      </header>

      <main class="workbench-grid">
        <section class="panel panel-primary">
          <slot name="primary" />
        </section>
        <section class="panel panel-secondary">
          <slot name="secondary" />
        </section>
      </main>

      <footer slot="footer">
        <small>v{package_json.version} · Built {build_time}</small>
      </footer>
    </wa-page>

    <script>
      import "@awesome.me/webawesome/dist/components/page/page.js";
      import "@awesome.me/webawesome/dist/components/button/button.js";
      import "@awesome.me/webawesome/dist/components/icon/icon.js";
      import "@aganitha/atk-ui";
    </script>

    <script is:inline>
      const themeBtn = document.getElementById("theme-toggle");
      const themeIcon = document.getElementById("theme-toggle-icon");
      const isDark = () => document.documentElement.classList.contains("wa-dark");
      const syncThemeIcon = () => {
        if (themeIcon) themeIcon.setAttribute("name", isDark() ? "sun" : "moon");
      };
      syncThemeIcon();

      if (themeBtn) {
        themeBtn.addEventListener("click", () => {
          const dark = document.documentElement.classList.toggle("wa-dark");
          localStorage.setItem("wa-theme", dark ? "dark" : "light");
          syncThemeIcon();
        });
      }
    </script>

    <style>
      html, body {
        min-height: 100%;
        margin: 0;
        padding: 0;
      }

      wa-page::part(main-content) {
        padding: var(--wa-space-l);
      }

      .brand {
        color: inherit;
        font-weight: var(--wa-font-weight-semibold);
        text-decoration: none;
      }

      .workbench-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: var(--wa-space-l);
        min-height: calc(100vh - 10rem);
        box-sizing: border-box;
      }

      @media (max-width: 1024px) {
        .workbench-grid {
          grid-template-columns: 1fr;
          min-height: auto;
        }
      }

      .panel {
        background-color: var(--wa-color-surface-raised);
        border: 1px solid var(--wa-color-surface-border);
        border-radius: var(--wa-border-radius-m);
        padding: var(--wa-space-m);
        overflow-y: auto;
      }

      .search-link {
        color: var(--wa-color-text-normal);
        text-decoration: none;
        font-size: var(--wa-font-size-s);
      }

      .search-link kbd {
        font-family: var(--wa-font-family-code);
        border: 1px solid var(--wa-color-surface-border);
        border-radius: var(--wa-border-radius-s);
        padding: 0 var(--wa-space-2xs);
      }
    </style>
  </body>
</html>
